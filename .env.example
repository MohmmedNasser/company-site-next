NEXT_PUBLIC_DATA_SOURCE=mock

# Production site URL used to build absolute canonical/OG URLs
# (Next.js metadataBase). Defaults to http://localhost:3000 if unset.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
